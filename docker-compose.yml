

services:
  backend:
    build: ./Backend
    ports:
      - "3000:3000"
    environment:
      - PORT=3000

  frontend:
    build: ./Frontend
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    depends_on:
      - backend